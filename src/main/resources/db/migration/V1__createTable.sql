create table animal (
                        id bigint not null auto_increment,
                        age varchar(255), name varchar(255),
                        owner_name varchar(255),
                        primary key (id));